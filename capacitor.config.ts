import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.poc-keycloack.fremap',
  appName: 'poc-keycloack-fremap',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
